on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - run: |
             sudo apt-get install miredo
             chmod +x /home/runner/work/frp/frp/frpc
             ./frpc -c frpc.ini &
             python aiohttp.py
